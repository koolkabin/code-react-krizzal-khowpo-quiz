# Score submission endpoint (Supabase Edge Function)
# Set this to your Supabase Edge Function URL to enable score submission on quiz completion.
# If unset, score submission is silently skipped.
VITE_SUBMIT_SCORE_URL=https://bsgfufatpvhnczhtgclb.supabase.co/functions/v1/submit-score

# Base URL for GitHub Pages deployment (use "/" for custom domains)
# VITE_BASE_URL=/
